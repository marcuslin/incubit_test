<p class="text-start fs-1">Sign In</p>
<%= simple_form_for :user, url: sign_in_path do |f| %>
  <%= f.input :email %>
  <%= f.input :password %>

  <%= f.submit 'Sign In', class: 'btn btn-outline-primary mt-1' %>
<% end %>
